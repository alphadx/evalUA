import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EvalUA v3.0 — Sistema de Evaluación Curricular por Rúbricas",
  description: "Micro-frontend autocontenido de evaluación curricular por rúbricas. Arquitectura DDD con Next.js 16, MongoDB y Redis.",
  keywords: ["EvalUA", "rúbricas", "evaluación", "curricular", "DDD", "micro-frontend"],
  authors: [{ name: "EvalUA Team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
